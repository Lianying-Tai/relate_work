# 交接规则

- 跨设备继续工作前，先读取 `.agent-handoff/START-HERE.md`。
- 需要更新交接状态时，优先使用 `agent-handoff close-session`。
- 如果当前环境没有 Python 或 PyYAML，可以直接阅读 `CURRENT.md`、`TASKS.md`、`DECISIONS.md` 和 `SESSION-LOG.md`。
- 本项目的正式代码入口是 `uav_ground_risk.py`，使用说明在 `README.md`。

